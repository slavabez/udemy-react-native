const initialState = {
    places: [],
    selectedPlace: null
};

const reducer = (state, action) => {
    switch(action.type){

        default:
            return state;
    }
};

export default reducer;